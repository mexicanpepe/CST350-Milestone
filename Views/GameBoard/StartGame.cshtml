@{
    ViewData["Title"] = "Start New Game";
}

<h2>@ViewData["Title"]</h2>

<form method="post" asp-controller="GameBoard" asp-action="StartGame">
    <button type="submit" class="btn btn-primary">Start New Game</button>
</form>
